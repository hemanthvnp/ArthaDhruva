# DEV ONLY (docker-compose.dev.yml): also allows plain TCP so a backend run on the host without
# TLS parameters can connect. Never mounted by the base compose file.
local   all   all                    trust
host    all   all   0.0.0.0/0        scram-sha-256
host    all   all   ::/0             scram-sha-256
